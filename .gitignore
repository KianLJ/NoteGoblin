node_modules/
out/
dist/
build/*.exe
*.log
.DS_Store
Thumbs.db

# runtime data — never commit a real user's campaign database, keys, or certs
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm
*.db
*.pem
*.key
*.crt
userdata/

# relay (separate project, see relay/) — local dev/build state, not source
relay/.partykit/
relay/.wrangler/

# TypeScript incremental build cache
*.tsbuildinfo

# electron-builder auto-loads this for GH_TOKEN (release publishing) — never commit it
electron-builder.env
